# EMA-Momentum Bot

This is a Python project for building an EMA-Momentum trading bot.

## Setup

1. Install dependencies:
   pip install -r requirements.txt

2. Run the bot:
   python src/main.py

## Description

The bot calculates Exponential Moving Average (EMA) and Momentum indicators for trading signals.
